# Examples

This folder contains optional material, not the main path.

```text
prior-runs/   archived JSON/JSONL outputs from earlier local runs
llama-cpp/    old GGUF helper files for plain llama.cpp chat
```

The SAE steering demo uses `chat_steer.py` with Hugging Face/Transformers bf16
weights, not the GGUF helper.
